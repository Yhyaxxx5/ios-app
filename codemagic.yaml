workflows:
  ios-build:
    name: iOS Build
    max_build_duration: 60
    instance_type: mac_mini_m1

    environment:
      xcode: latest

    scripts:
      - name: Extract source code
        script: |
          tar -xf ios_source.tar.gz

      - name: Install dependencies
        script: |
          npm install
